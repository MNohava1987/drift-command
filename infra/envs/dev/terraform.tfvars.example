# Copy this to terraform.tfvars and fill in your values.
# Do NOT commit terraform.tfvars — it is gitignored.

project_id  = "your-gcp-project-id"
region      = "us-central1"
github_repo = "MNohava1987/drift-command"
