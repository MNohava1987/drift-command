// Command topology system was removed in the M5→M6 refactor.
// Tests are preserved as a stub to keep the test file valid.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('command topology system removed — placeholder', () {
    // The relay/topology system was removed. Ships receive orders directly.
    expect(true, isTrue);
  });
}
